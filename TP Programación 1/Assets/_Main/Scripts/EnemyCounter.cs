/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyCounter : MonoBehaviour
{
    public int enemysammount = 1;
    public int currentenemyammount;
    // Start is called before the first frame update
    void Start()
    {
        currentenemyammount = 0;
    }

    // Update is called once per frame
    void Update()
    {
        if (currentenemyammount >= enemysammount)
        {
            Debug.Log("Hay enemigos muertos");
        }
    }
}
*/